# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*~

# Build / dist
*.zip
*.tar.gz
_deploy/
dist/
build/
node_modules/

# Logs
*.log

# Env
.env
.env.*

# AI & Backup folders — committed to repo but NOT deployed via GitHub Actions
# (Excluded in .github/workflows/deploy.yml via the _deploy copy step)
# These are intentionally kept in the repo for version history and AI continuity.
# DO NOT add .Backups/ or .files-for-ai/ here — they must stay in version control.
