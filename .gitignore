# ── OS files ──────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ── Editor / IDE ───────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.project
.settings/

# ── Build artifacts ────────────────────
*.zip
*.tar.gz
_deploy/
dist/
build/

# ── Logs ───────────────────────────────
*.log
npm-debug.log*

# ── Dependencies (future) ──────────────
node_modules/
.npm/

# ── Environment (future) ───────────────
.env
.env.local
.env.*.local

# ── Development ────────────────────────
.Backups
.files-for-ai


# ── GitHub Pages deploy artifact ───────
# (The _deploy folder is used by workflow, not committed)
_deploy/

# NOTE: .Backups/ and .files-for-ai/ ARE committed to the repo intentionally.
# They are excluded from the GitHub Pages deploy via the workflow, not .gitignore.
